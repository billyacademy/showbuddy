<h1> Users </h1>


  <h1><% @users.each do |user| %></h1>
    <%= link_to user.name, user_path(user), class: 'button' %>
    <%= link_to "Delete #{user.name}", admin_user_path(user), method: :delete,
    data: { confirm: "Are you sure?"}, class: 'alert button' %><br>
  <% end %>
